Golang ius a **compiled language**.
( Compiled - Whole code is read first, then it creates an **exe** & then it runs. It is a **fast process**. )

We can build **System apps to Web apps** using Golang.
